#include "stroki.h"
using namespace std;

string itc_hello_str(string name) {
	return "Hello, " + name;
}

long long itc_len(string str) {
	int kol, i;
	kol = 0;
	i = 0;
	while (str[i] != '\0') {
		kol++;
		i++;
	}
	return kol;
}

void itc_print_copy_str(string str, int number) {
	for(str = 1;str < number; str++)
}

void itc_first_end_three(string str) {
	if (str[i] < 5)

	

}